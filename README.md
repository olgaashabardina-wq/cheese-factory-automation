СЫРОВАРНЯ — АВТОМАТИЗАЦИЯ ПРОИЗВОДСТВА И ПРОДАЖ
Описание проекта: 
Интеллектуальная система автоматизации для ремесленной сыроварни.  
Проект объединяет Telegram-бота, Google Таблицы, Albato и PythonAnywhere (Flask API)  
для управления закупкой сырья, производством и складом готовой продукции.  

Цель проекта:
Создать простую и эффективную систему учёта сырья, производства и продаж,  
используя инструменты без кода (Albato, Google Sheets) и API на Python.

Логика работы системы
Клиент оформляет заказ в Telegram-боте (например: `сулугуни 5 кг`).  
Albato:
   - получает сообщение,  
   - парсит его через регулярные выражения,  
   - создаёт запись в листе «Резервы» Google Таблицы.
Таблица автоматически обновляет:
   - остатки в «Складе сырья», 
   - и передаёт результат в «Склад готовой продукции».
Когда партия готова — бот отправляет сообщение пользователю:
 “Партия готова. Отгрузка завтра.”
 Все обновления фиксируются в Google Sheets в реальном времени.

Используемые технологии
Компонент/Назначение 
Python + Flask /Веб-сервер и API-обработчик запросов
Albato/Автоматизация связок Telegram → Google Sheets
Google Sheets/Центральное хранилище данных (сырьё, заказы, склады)
PythonAnywhere/Хостинг и развёртывание Flask-сервера 
GitHub/Публикация проекта и документации

API-ФУНКЦИОНАЛ НА PYTHON (Flask)

Файл **`app.py`** содержит следующие эндпоинты:

```python
@app.route("/api/health", methods=["GET"])
def health():
    return jsonify(ok=True, message="Cheese Factory API working!"), 200

@app.route("/api/reserve", methods=["POST"])
def reserve():
    data = request.get_json(force=True)
    return jsonify(ok=True, received=data), 200

@app.route("/api/order/<order_id>", methods=["GET"])
def order_status(order_id):
    return jsonify(order_id=order_id, status="reserved"), 200

СТРУКТУРА ПРОЕКТА
cheese-factory-automation
 📜 app.py   — Flask API-сервер
📜 requirements.txt  — зависимости Python
 📜 Procfile  — инструкция запуска сервера
📜 .gitignore          — исключения и защита от утечек
README.md           — документация проекта
консоль.png         — скриншот проверки API на PythonAnywhere

СВЯЗКИ В ALBATO
Вариант А — приём заказов:
Триггер: Telegram (входящее сообщение)
Шаги:
1️ Парсер значений (регулярные выражения)
2️ Декодирование JSON
3️ Создание строки в листе «Резервы»
4️ Обновление статуса и уведомление в Telegram
Вариант B — обновление складов и производства:
Триггер: обновление строки в листе «Производство»
Шаги:
1️ Справочник продукции (расчет цены за кг.)
2 Производство (создание партии)
3 Склад сырья (списание сырья)
4 Склад готовой продукции (поступление партии)
5 HTTP-запрос к Flask-серверу /api/reserve

ОТЧЁТ О ВЫПОЛНЕНИИ
Что реализовано:
✅ Полная интеграция Telegram → Albato → Google Sheets
✅ Flask-сервер с API /api/health и /api/reserve
✅ Настроен деплой на PythonAnywhere
✅ Создан GitHub-репозиторий с документацией и кодом
Что планируется улучшить:
🔹 Добавить API-интерфейс для учёта отгрузок.
🔹 Реализовать аналитические отчёты.
🔹 Отладить связи в Albato, так как инструмент нестабилен и требует тестирования.

ПРИМЕРЫ СКРИНОВ ДЛЯ ДОКУМЕНТАЦИИ
✅ Telegram-бот — получение и подтверждение заказа
✅ Albato — шаги связки и тестовые данные
✅ Google Sheets — обновление складов и производства
✅ PythonAnywhere — проверка кода через /api/health

АВТОР ПРОЕКТА
Ольга Шабардина
Prompt Engineer
📍 Москва
📧 Telegram: @VillicaBot
🔗 GitHub: olgaashasbardina-wq

Примеры скринов
telegram.png
albato.png
sheets.png
pythonanywhere.png



